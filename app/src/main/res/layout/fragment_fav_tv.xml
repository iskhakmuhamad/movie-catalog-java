<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:paddingTop="@dimen/activity_vertical_frame"
    android:paddingBottom="@dimen/activity_vertical_frame"
    android:paddingStart="@dimen/activity_horizontal_frame"
    android:paddingEnd="@dimen/activity_horizontal_frame"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorDarkWhite"
    android:orientation="vertical"
    tools:context=".view.favfragmentviewpager.FavTvFragment">

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_fav_tv"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:listitem="@layout/item_card_row" />
    <TextView
        android:id="@+id/tv_null_tv"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fontFamily="monospace"
        android:gravity="center"
        android:text="@string/nothing_favorit_tv_show"
        android:textSize="20sp"
        android:textStyle="bold" />

</LinearLayout>